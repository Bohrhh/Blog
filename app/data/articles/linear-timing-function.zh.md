# 原生 CSS 中的弹簧与弹跳效果

`linear()` 时间函数是一个改变游戏规则的功能，它让我们能够在纯 CSS 中模拟基于物理的运动！

## 理解 linear()

linear() 函数允许你创建模拟基于物理的动画的自定义缓动效果。

```css
.card {
  animation: bounce 0.5s linear(0, 0.5, 0.5, 1.2, 1);
}
```

## 浏览器支持

此功能现在已被所有主流浏览器支持。

## 结论

去创造动画吧！
