# SVG 入门指南

SVG 是我们可以在网页上使用的最出色的技术之一。

## 什么是 SVG？

SVG 代表可缩放矢量图形。它们是一等公民，可以完全用 CSS 和 JavaScript 进行操作。

## 入门指南

```html
<svg viewBox="0 0 100 100">
  <circle cx="50" cy="50" r="40" />
</svg>
```

## 使用 CSS 样式化

你可以像对待常规 HTML 元素一样为 SVG 元素添加样式！

## 结论

去探索 SVG 的精彩世界吧！
